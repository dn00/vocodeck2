// Vendored highlight.js 11.10.0 (BSD-3-Clause) as an ES module. Buildless:
// the browser bundle sets globalThis.hljs, which we re-export. Common-
// languages build (the reference file-viewer's degrade path uses hljs too).
// See vendor/MANIFEST.
import "./highlight.min.js";
export const hljs = globalThis.hljs;
