// Vendored marked 14.1.3 (MIT) as an ES module. Buildless: the UMD bundle
// sets globalThis.marked, which we re-export. See vendor/MANIFEST.
import "./marked.min.js";
export const marked = globalThis.marked;
